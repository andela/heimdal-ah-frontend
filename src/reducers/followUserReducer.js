// import { FOLLOW_USER } from '../actions/actionTypes';

const initialState = {
  message: '',
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
